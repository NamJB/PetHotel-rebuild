<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pethotel.mapper.MypageMapper">
   <select id = "myList" resultType = "com.pethotel.dto.BoardDto">
      select 
      b.id,
      b.title,
      b.content,
      b.created_at,
      m.user_id
      from board b inner join member m on b.member_id = m.id
      where member_id = #{member_id} 
      order by b.id desc
   </select>
   
   <select id = "myRes">
      select * 
      from reservation 
      where member_id = #{member_id}
      order by id desc
      
   </select>
</mapper>